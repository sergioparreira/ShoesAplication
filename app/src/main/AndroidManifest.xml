<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/tenis"
        android:label="@string/app_name"
        android:roundIcon="@drawable/tenis"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoesAplication"
        tools:targetApi="31">
        <activity
            android:name="com.aplication.maxcomposeshoes.presentation.main.activity.MainScreen"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ShoesAplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.aplication.maxcomposeshoes.presentation.detalhes.activity.DetalheScreen"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ShoesAplication">
        </activity>
    </application>

</manifest>